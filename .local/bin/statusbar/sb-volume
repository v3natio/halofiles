#!/bin/sh

[ $(pamixer --get-mute) = true ] && echo " 󰝟 --%" && exit

VOL="$(pamixer --get-volume)"

if [ "$VOL" -gt "50" ]; then
	echo " 󰕾 ${VOL}%"
elif [ "$VOL" -gt "20" ]; then
  echo " 󰖀 ${VOL}%"
elif [ "$VOL" -gt "0" ]; then
	echo " 󰕿 ${VOL}%"
else
        echo " 󰝟 --%" && exit
fi
